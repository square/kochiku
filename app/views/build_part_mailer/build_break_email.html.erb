<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
</head>
<body>

Emails this would have been sent to:
<ul>
<% @emails.each do |email| %>
  <li><%= email %></li>
<% end %>
</ul>

Build Part for <%= @build_part.project.name %> failed

Go to <%= project_build_part_url(@build_part.build_instance.project, @build_part.build_instance, @build_part) %> for details

Links to log files
<ul>
<% @artifacts.each do |artifact| %>
  <li><%= link_to(File.basename(artifact.log_file.to_s), artifact.log_file.url) %></li>
<% end %>
</ul>

<h1>Changes since last success</h1>

<%= @git_changes %>

</body>
</html>