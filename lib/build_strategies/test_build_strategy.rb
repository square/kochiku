class BuildStrategy
  class << self
    def promote_build(build)
    end
    def merge_ref(ref)
    end
    def run_success_script(build)
    end
    def add_note(build_ref, namespace, note)
    end
  end
end
